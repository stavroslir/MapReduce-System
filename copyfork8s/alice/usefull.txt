Deploy Alice:

kubectl apply -f curl-pod.yaml


Exec in the pod(to test):
kubectl exec -it alice -- /bin/sh

Curl command example:

curl -X POST -H "Content-Type: application/json" -d '{"username":"admin", "password":"adminpassword"}' http://ui-service:7000/login
